coverage:
  status:
    project:
      default:
        # Tolerate small fluctuations (e.g. refactors that change what's in
        # scope) instead of hard-failing on any decrease.
        threshold: 2%
    patch:
      default:
        threshold: 1%
